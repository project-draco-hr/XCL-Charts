{
  if (getVisible() && getAxisLineVisible()) {
    if (null != mColor)     getFillAxisPaint().setColor(mColor.get(0));
    DrawHelper.getInstance().drawPercent(canvas,this.getFillAxisPaint(),mCirX,mCirY,mRadius,mInitAngle,mTotalAngle,true);
  }
  return true;
}
