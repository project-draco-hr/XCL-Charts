{
  try {
    super.postRender(canvas);
    calcPlotRange();
    plotArea.render(canvas);
    renderTitle(canvas);
    axisTitle.setRange(this);
    axisTitle.render(canvas);
  }
 catch (  Exception e) {
    throw e;
  }
  return true;
}
