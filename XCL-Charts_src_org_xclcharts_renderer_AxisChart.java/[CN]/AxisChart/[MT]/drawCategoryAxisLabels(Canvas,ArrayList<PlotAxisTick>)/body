{
  if (null == lstLabels)   return;
  for (  PlotAxisTick t : lstLabels) {
switch (mCategoryAxisPosition) {
case LEFT:
case RIGHT:
      categoryAxis.renderAxisHorizontalTick(this,canvas,t.X,t.Y,t.Label,isDrawYAxisTickMarks(t.Y,mMoveY));
    break;
case TOP:
case BOTTOM:
  categoryAxis.renderAxisVerticalTick(canvas,t.X,t.Y,t.Label,isDrawXAxisTickMarks(t.X,mMoveX));
break;
}
}
}
