{
  try {
    if (null == canvas)     return false;
    super.render(canvas);
    renderFocusShape(canvas);
    renderToolTip(canvas);
    return true;
  }
 catch (  Exception e) {
    throw e;
  }
}
