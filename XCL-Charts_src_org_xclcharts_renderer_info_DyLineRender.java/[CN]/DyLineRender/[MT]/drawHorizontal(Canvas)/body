{
  float startX=mArea.getLeft();
  float startY=mCenterXY.y;
  float stopX=mArea.getRight();
  float stopY=mCenterXY.y;
  DrawHelper.getInstance().drawLine(getLineDrawStyle(),startX,startY,stopX,stopY,canvas,getLinePaint());
}
