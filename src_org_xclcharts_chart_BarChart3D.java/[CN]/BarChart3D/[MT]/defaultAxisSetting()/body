{
  super.defaultAxisSetting();
  try {
switch (getChartDirection()) {
case HORIZONTAL:
{
        labelsAxis.setTickMarksVisible(false);
        break;
      }
case VERTICAL:
{
      labelsAxis.setTickMarksVisible(false);
      break;
    }
}
}
 catch (Exception ex) {
Log.e("ERROR-Bar3DChart",ex.toString());
}
}
