{
  if (false == getVisible())   return;
  float marksStartY=centerY;
  float marksStopY=centerY;
  float lablesStartY=centerY;
switch (getAxisVerticalTickPostion()) {
case UP:
{
      if (getAxisTickMarksVisible()) {
        marksStartY=Math.round(centerY - getTickMarksLength());
        marksStopY=centerY;
        lablesStartY=marksStartY - getTickLabelsMargin() - mDrawHelper.getPaintFontHeight(getAxisTickLabelsPaint());
      }
      break;
    }
case CENTER:
{
    if (getAxisTickMarksVisible()) {
      marksStartY=Math.round(centerY - getTickMarksLength() / 2);
      marksStopY=Math.round(centerY + getTickMarksLength() / 2);
    }
    break;
  }
case LOWER:
if (getAxisTickMarksVisible()) {
  marksStartY=centerY;
  marksStopY=Math.round(centerY + getTickMarksLength());
  lablesStartY=marksStopY + getTickLabelsMargin() + mDrawHelper.getPaintFontHeight(getAxisTickLabelsPaint()) / 3;
}
break;
default :
break;
}
if (getAxisTickMarksVisible()) {
mCanvas.drawLine(centerX,marksStartY - this.getAxisPaint().getStrokeWidth() / 2,centerX,marksStopY,getAxisTickMarksPaint());
}
if (getAxisTickLabelsVisible()) {
String itemLabel="";
try {
itemLabel=mLabelFormatter.textFormatter(text);
}
 catch (Exception ex) {
itemLabel=text;
}
mDrawHelper.drawRotateText(itemLabel,centerX,lablesStartY,getAxisTickLabelsRotateAgent(),mCanvas,getAxisTickLabelsPaint());
}
}
