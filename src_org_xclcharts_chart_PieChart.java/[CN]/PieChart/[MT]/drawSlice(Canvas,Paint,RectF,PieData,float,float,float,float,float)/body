{
  try {
    canvas.drawArc(arcRF0,offsetAgent,curretAgent,true,paintArc);
    drawLables(canvas,cData.getLabel(),cirX,cirY,radius,offsetAgent,curretAgent);
  }
 catch (  Exception e) {
    throw e;
  }
}
