{
  if (per < 50) {
    mDataInfo="????????????";
  }
 else   if (per < 70) {
    mDataInfo="????????????";
  }
 else {
    mDataInfo="????????????";
  }
  mlPieData.clear();
  mlPieData.add(new PieData(Integer.toString(per) + "%",per,(int)Color.rgb(72,201,176)));
}
