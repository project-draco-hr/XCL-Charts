{
  if (null == mLstPoints) {
    mLstPoints=new ArrayList<PointF>();
  }
 else {
    mLstPoints.clear();
  }
  if (null == mMapPoints) {
    mMapPoints=new LinkedHashMap<PlotLine,List<PointF>>();
  }
 else {
    mMapPoints.clear();
  }
}
