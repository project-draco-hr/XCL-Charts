{
  boolean ret=true;
  try {
    if (null == canvas)     return false;
    canvas.save();
    canvas.translate(mTranslateXY[0],mTranslateXY[1]);
    super.render(canvas);
    canvas.restore();
    return true;
  }
 catch (  Exception e) {
    throw e;
  }
}
