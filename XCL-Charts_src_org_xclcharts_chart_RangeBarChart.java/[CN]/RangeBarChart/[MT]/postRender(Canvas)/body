{
  try {
    super.postRender(canvas);
    if (null == mDataSet) {
      Log.e(TAG,"????????????????????????");
      return false;
    }
    List<String> dataSet=categoryAxis.getDataSet();
    if (null == dataSet) {
      Log.e(TAG,"????????????????????????.");
      return false;
    }
    return renderVerticalBar(canvas);
  }
 catch (  Exception e) {
    throw e;
  }
}
