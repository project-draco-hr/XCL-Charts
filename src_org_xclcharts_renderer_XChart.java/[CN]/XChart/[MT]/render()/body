{
  try {
    if (null == mCanvas)     return false;
    drawChartBackgroup();
    plotArea.setCanvas(mCanvas);
    plotGrid.setCanvas(mCanvas);
  }
 catch (  Exception e) {
    throw e;
  }
  return true;
}
