{
  try {
    renderDount(canvas);
    renderTick(canvas);
    renderPartitionFill(canvas);
    renderLabels(canvas);
    renderPointerLine(canvas);
    renderPinterCircle(canvas);
  }
 catch (  Exception e) {
    Log.e("ERROR",e.toString());
  }
}
