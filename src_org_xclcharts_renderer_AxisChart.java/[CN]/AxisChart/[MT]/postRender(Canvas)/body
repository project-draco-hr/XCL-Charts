{
  try {
    calcPlotRange();
    plotArea.render(canvas);
    renderTitle(canvas);
    legend.setRange(this);
    legend.render(canvas);
  }
 catch (  Exception e) {
    throw e;
  }
  return true;
}
