{
  try {
    super.postRender(canvas);
    if (getPanModeStatus()) {
      return drawClipVerticalPlot(canvas);
    }
 else {
      return drawVerticalPlot(canvas);
    }
  }
 catch (  Exception e) {
    throw e;
  }
}
