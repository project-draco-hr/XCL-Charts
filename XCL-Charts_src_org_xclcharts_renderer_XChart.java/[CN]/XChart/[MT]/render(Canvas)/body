{
  boolean ret=true;
  try {
    if (null == canvas)     return false;
    scaleChart(canvas);
    ret=postRender(canvas);
    renderBorder(canvas);
    return ret;
  }
 catch (  Exception e) {
    throw e;
  }
}
