{
  if (startX > 0)   mLeft=startX;
  if (startY > 0)   mTop=startY;
  mRight=add(startX,width);
  mBottom=add(startY,height);
  if (Float.compare(width,0.0f) > 0)   mWidth=width;
  if (Float.compare(height,0.0f) > 0)   mHeight=height;
}
