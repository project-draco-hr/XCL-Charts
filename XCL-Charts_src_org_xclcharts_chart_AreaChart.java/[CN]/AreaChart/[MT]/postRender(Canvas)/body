{
  try {
    super.postRender(canvas);
    if (getPanModeStatus()) {
      drawClipVerticalPlot(canvas);
    }
 else {
      drawVerticalPlot(canvas);
    }
    renderFocusShape(canvas);
    return true;
  }
 catch (  Exception e) {
    throw e;
  }
}
