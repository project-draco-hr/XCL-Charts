{
  if (false == this.getPlotKeyVisible())   return;
switch (this.getPlotTitle().getChartTitleAlign()) {
case CENTER:
case RIGHT:
    drawDataSetKeyLeft(canvas);
  break;
case LEFT:
drawDataSetKeyRight(canvas);
break;
}
}
