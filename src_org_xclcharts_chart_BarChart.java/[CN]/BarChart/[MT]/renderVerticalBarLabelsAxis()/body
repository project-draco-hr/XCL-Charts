{
  float currentX=plotArea.getPlotLeft();
  List<String> dataSet=labelsAxis.getDataSet();
  float XSteps=getAxisScreenWidth() / (dataSet.size() + 1);
  for (int i=0; i < dataSet.size(); i++) {
    currentX=plotArea.getPlotLeft() + (i + 1) * XSteps;
    if (plotGrid.getVerticalLinesVisible()) {
      this.mCanvas.drawLine(currentX,plotArea.getPlotBottom(),currentX,plotArea.getPlotTop(),plotGrid.getVerticalLinesPaint());
    }
    labelsAxis.renderAxisVerticalTick(currentX,plotArea.getPlotBottom(),dataSet.get(i));
  }
}
