{
  if (false == this.isShowKeyLabels())   return;
switch (this.getPlotTitle().getTitleAlign()) {
case CENTER:
case RIGHT:
    renderKeyLeft(canvas);
  break;
case LEFT:
renderKeyRight(canvas);
break;
}
}
