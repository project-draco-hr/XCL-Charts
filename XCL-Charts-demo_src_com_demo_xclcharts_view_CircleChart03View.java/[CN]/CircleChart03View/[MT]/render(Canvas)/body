{
  try {
    canvas.drawColor(Color.GRAY);
    chart.setChartRange(10f,10f,this.getLayoutParams().width - 10,this.getLayoutParams().height - 10);
    chart.setPadding(120,180,100,100);
    chart.render(canvas);
  }
 catch (  Exception e) {
    Log.e(TAG,e.toString());
  }
}
